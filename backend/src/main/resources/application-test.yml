spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/botica_inteligente_test
    username: tc
    password: tc
  jpa:
    show-sql: true
    properties:
      hibernate:
        default_schema: botica
  security:
    jwt:
      secret: ${JWT_SECRET:test_secret_key_that_must_be_long_enough_for_hs256}
      expiration: ${JWT_EXPIRATION:3600000}
